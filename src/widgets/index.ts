/**
 * Page-level widgets — compose features + shared components.
 * Only `app/` and other widgets should import from here.
 */
export { default as Home } from './home';
export { default as Main } from './main';
